This is used to add a intervention
---
tags:
  - Interventions
parameters:
  - in: body
    name: body
    required: true
    type: string
    description: This enables a user to add a intervention to the database
responses:
  201:
    desciption: Intervention created successfully
    schema:
      properties:
        name:
          type: string
          default: intervention
  400:
    description: Request cannot be left empty
  401:
    description: Missing authorization header!
security:
  - bearerAuth:
      description: Pass the jwt token e.g Bearer <token>
      type: http
      scheme: bearer
      in: header
      bearerFormat: JWT 