This is used to get a red-flag
---
tags:
  - Red-flags
responses:
  201:
    description: Succesfully retrieved redflags
  404:
    description: There are no red-flags
security:
  - bearerAuth:
      description: Pass the jwt token e.g Bearer <token>
      type: http
      scheme: bearer
      in: header
      bearerFormat: JWT