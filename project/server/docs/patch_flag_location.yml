This will edit the location of a given red-flag
---
tags:
  - Red-flags
parameters:
  - in: path
    name: body
    required: true
    type: integer
    description: Enter red-flag id
  - in: body
    name: body
    required: true
    type: string
    description: Enter new location
responses:
  201:
    description: Location updated
    schema:
      properties:
        name:
          type: string
          default: location
  400:
    description: Failed to update location
  401:
    description: Missing authorization header
security:
  -  bearerAuth:
        description: Pass the jwt token e.g Bearer <token>
        type: http
        scheme: bearer
        in: header
        bearerFormat: JWT